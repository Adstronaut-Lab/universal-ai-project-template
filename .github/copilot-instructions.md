# Copilot Instructions

Read `AGENTS.md` first.

Use concise, result-first answers. Read files before edits. Prefer targeted changes. Avoid speculative APIs, unnecessary abstractions, preambles, and closing fluff. Run the narrowest useful validation and report only what changed.
