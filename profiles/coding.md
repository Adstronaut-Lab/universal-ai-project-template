# Coding Profile

Use with `AGENTS.md` for development, debugging, review, and refactoring.

- Code first when code is the answer.
- Explain after code only when non-obvious.
- Use existing project style.
- Avoid new dependencies unless required.
- In reviews, lead with bugs and file references.
- In debugging, state what was checked and what failed.
