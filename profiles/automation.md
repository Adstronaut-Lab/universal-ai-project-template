# Automation Profile

Use with `AGENTS.md` for scripts, pipelines, and multi-agent loops.

- Output structured data when a machine consumes it.
- Do not narrate tool usage.
- Return `UNKNOWN` for missing values.
- Stop on failed required steps and report the exact failure.
- Keep payloads minimal and parseable.
